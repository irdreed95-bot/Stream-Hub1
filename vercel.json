{
  "buildCommand": "pnpm install && cd artifacts/streamvault && pnpm run build",
  "outputDirectory": "artifacts/streamvault/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
