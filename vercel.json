{
  "buildCommand": "cd artifacts/streamvault && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/streamvault/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "root": "artifacts/streamvault",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
